networks:
  biggr-net:
    name: biggr-net
    external: false

services:
  cobradb-test:
    build: .
    depends_on:
      biggr-db:
        condition: service_healthy
    networks:
      - biggr-net
    environment:
      LIBCHEBIPY_DOWNLOAD_DIR: "/chebi" 
    volumes:
      - /data/biggr-genbank:/genbank
      - /data/biggr-assemblies:/assemblies
      - /data/biggr-bigg-models:/models
      - /data/biggr-input-models:/input_models
      - /data/biggr-chebi-cache:/chebi
      - /data/biggr-datafiles:/datafiles
    stdin_open: true
    tty: true
  biggr-db:
    container_name: cobradb-postgres
    image: postgres:17.6
    command: -c config_file=/etc/postgresql.conf
    restart: always
    volumes:
      - /data/biggr-postgres:/var/lib/postgresql/data
      - ./postgresql.conf:/etc/postgresql.conf
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready"]
      interval: 1s
      timeout: 5s
      retries: 10
    env_file:
      - .env
    networks:
      - biggr-net
